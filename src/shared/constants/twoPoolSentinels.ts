/** Obvious string default when indexer / subgraph has no value. */
export const TWO_POOL_NOT_DEFINED_STR = "Not defined";

/** Obvious numeric default when indexer has no value (not a valid APY % or fee %). */
export const TWO_POOL_NOT_DEFINED_NUM = -1;
