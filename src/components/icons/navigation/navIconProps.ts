/** Bottom navigation tab icons (active tint). */
export type BottomNavIconProps = {
  className?: string;
  active?: boolean;
};

/** Header chrome icons (settings, notifications). */
export type HeaderChromeIconProps = {
  className?: string;
};
